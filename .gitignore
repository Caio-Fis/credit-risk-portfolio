# Ambiente virtual
.venv/
venv/

# Dados brutos (nunca versionar)
data/raw/

# Artefatos MLflow
mlruns/
mlartifacts/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Variáveis de ambiente
.env
.env.*
kaggle.json

# IDEs
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Coverage
.coverage
htmlcov/
.pytest_cache/

# Ruff cache
.ruff_cache/

# Feature store (45MB — gerada por make features, não vai para o repo)
data/processed/feature_store.parquet

# v2 derived parquets — regenerable via `make pipeline-lc`
data/processed/lendingclub_*.parquet
data/processed/frozen_*.parquet
data/processed/oos_predictions_lc.parquet
data/processed/arf_predictions_lc.parquet

# Claude Code local settings / state
.claude/

# Notas pessoais de sessão (backlog, todo, lições, redesigns) — só local
tasks/

# Logs
logs/

# uv lockfile — requirements.txt already pins all deps for deployment
uv.lock
